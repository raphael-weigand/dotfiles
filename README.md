This is the current configuration on the mac. You must create the paths (~/dotfiles, ~/.config/<application>) yourself.

ToDo: Create a setup script which makes the links to the respective .config folder.
