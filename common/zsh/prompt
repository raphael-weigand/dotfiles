setopt PROMPT_SUBST

autoload -Uz colors && colors

if (( $+functions[git_prompt_segment] )); then
    PROMPT='%F{yellow}%~%f$(git_prompt_segment)\n%F{white}>%f '
else
    PROMPT='%F{yellow}%~%f\n%F{white}>%f '
fi

RPROMPT=''
